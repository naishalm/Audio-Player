// StatusComponent.js
import React from 'react';

const StatusComponent = ({ status }) => {
    return (
        <div>
            <p>{status}</p>
        </div>
    );
};

export default StatusComponent;
